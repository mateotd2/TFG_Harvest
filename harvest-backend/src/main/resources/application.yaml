# https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.external-config.files



server:
  port: 8080
#  ;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE



spring:
  profiles:
    active: @activatedProperties@
  sql:
    init:
      data-locations: classpath:data.sql # Fichero que puebla la base de datos para los test
      platform: mysql
#      mode: always     # Siempre inicializa la base de datos



  datasource:
    url: jdbc:mysql://localhost:3306/harvest_db?useSSL=false
#    url: jdbc:mysql://localhost:3306/harvest_db?allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: userHarvest
    password: harvestland



  jpa:
    defer-datasource-initialization: true
    hibernate:

      show_sql: true
      format_sql: true
#      ddl-auto: create-drop # Crea y dropea las tablas, no utilizar en un entorno de produccion
#      ddl-auto: create # Crea tablas a partir de las entidades... USAR LA PRIMERA VEZ
      ddl-auto: update
#      ddl-auto: none # MUCHOS FALLOS AQUI
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect  # Selecciona el dialecto específico de la base de datos

app:
  jwtcookie: Bu:GW8bgPlEw
  jwtSecret: harvestSpringharvestSpringharvestSpringharvestSpringharvestSpringharvestSpringharvestSpringharvestSpringharvestSpringharvestSpringharvestSpringharvestSpring
  jwtExpirationMs: 86400
  jwtRefreshExpirationMs: 86400000

springdoc:
  show-login-endpoint: true
  #  api-docs:
  #    version: openapi_3_0

  swagger-ui:
    urlsPrimaryName: /v3/api-docs
    #    docExpansion: list
    persistAuthorization: true
    use-root-path: true
    disable-swagger-default-url: true
    tryItOutEnabled: true



#logging:
#  level:
#    org.hibernate.SQL: debug   # Establece el nivel de registro para las sentencias SQL a "info"
#    org.hibernate.type: debug  # Establece el nivel de registro para los tipos de datos de Hibernate a "info"

